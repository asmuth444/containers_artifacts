apiVersion: v1
kind: Service
metadata:
  name: oh-userprofile-service
  namespace: oh-backend
spec:
  type: ClusterIP
  selector:
    app: oh-userprofile
  ports:
  - name: http
    port: 80
    targetPort: 80

