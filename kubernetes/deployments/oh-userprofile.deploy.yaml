apiVersion: apps/v1
kind: Deployment
metadata:
  name: oh-userprofile
  namespace: oh-backend
spec:
  selector:
    matchLabels:
      app: oh-userprofile
  template:
    metadata:
      labels:
        app: oh-userprofile
    spec:
      containers:
      - name: userprofile
        image: registryznq7406.azurecr.io/openhack/userprofile:latest
        env:
        - name: "SQL_USER"
          valueFrom:
            secretKeyRef:
              name: "openhacksecret"
              key: sql_user
        - name: "SQL_PASSWORD"
          valueFrom:
            secretKeyRef:
              name: "openhacksecret"
              key: sql_password
        - name: "SQL_SERVER"
          valueFrom:
            secretKeyRef:
              name: "openhacksecret"
              key: sql_server
        - name: "SQL_DBNAME"
          valueFrom:
            secretKeyRef:
              name: "openhacksecret"
              key: sql_dbname
        resources:
          limits:
            memory: "128Mi"
            cpu: "100m"
        ports:
        - containerPort: 80